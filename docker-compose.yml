services:
  lidarr-onsteroids:
    image: solidshop/lidarr-on-steroids
    build:
      context: .
    environment:
      - FLAC2CUSTOM_ARGS=-a "-vn -c:a libopus -b:a 64k" -e .opus -r '\.mp3$$'
    ports:
      - "8686:8686" # Lidarr web UI
      - "6595:6595" # Deemix web UI
      - "7171:7171" # Track-picker sidecar (Lidarr UI calls this from the browser)
    volumes:
      - ./tmp/config:/config
      - ./tmp/config_deemix:/config_deemix
      - ./tmp/downloads:/downloads
      - ./tmp/music:/music
    restart: always
